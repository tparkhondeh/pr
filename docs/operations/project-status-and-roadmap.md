# PR — آخرین وضعیت و قدم بعد

## خروجی امن و دریافت مجدد — ۱۶ سپتامبر ۲۰۲۶

مرجع انتشار نهایی همان رسید محلی/سرور `latest-release.json` است؛ تا تغییر آن از `fc4d1b9`، این مرحله را منتشرشده فرض نکنید. baseline این نوبت با GitHub و SOURCE_COMMIT سرور تطبیق داشت و تغییر محصول ثبت‌نشده‌ای وجود نداشت.

- مرحلهٔ انتخاب‌شده: اتصال امن ویرایش → ذخیره → تأیید → دریافت فایل → بازخورد. ویرایش ذخیره‌نشده تأیید/خروجی را می‌بندد؛ به‌روزرسانی، متن در حال ویرایش را بی‌صدا عوض نمی‌کند؛ اختلاف نسخه نیازمند تطبیق صریح است. ترک بخش هنوز متن ذخیره‌نشده را نگه نمی‌دارد و هشدار دارد.
- دریافت دوبارهٔ همان نسخهٔ صادرشده پس از کنترل مجدد مالک، نسخه، شاهد، ادعا و استراتژی مجاز است؛ نباید تأیید/رویداد خروجی تازه یا بازخورد خودکار بسازد. دریافت فایل به معنی انتشار، رضایت یا پذیرش محصول نیست.
- مسئولیت واقعی: `product_next_step` سرویس خروجی و آزمون backend/PostgreSQL؛ `ux_implementation` رابط و مدل خالص وضعیت و آزمون؛ `security_review` کنترل مستقل هم‌زمانی و فضای دیسک؛ root پشتیبان، یکپارچه‌سازی، مرورگر و انتشار. بازبین اشکال دریافت نسخهٔ قدیمی حین ویرایش هم‌زمان را یافت؛ اصلاح و آزمون regression پیش‌نیاز انتشار است.
- پشتیبان قبل از تغییر: `C:\Users\pc\Desktop\pr-audit-backups\pr-before-next-20260916-120042.tar.gz`، SHA256 `29F6C6ECA3FA468143F74E9E815255BDAFB5FD4B12712BF867AB58E60298C64F`.
- فضای سرور ابتدا 1,213,984,768 بایت بود. شش staging قدیمی با شناسه‌های `6f8dcc4`, `9160686`, `f695606`, `4be186e`, `87d5e6d`, `d0bf7ac` پس از تطبیق همهٔ فایل‌ها با `dist/cpanel-SHA` محلی (صفر اختلاف SHA256 و بدون symlink) حذف شدند؛ آرشیو متناظر محلی و `.deploy-incoming-SHA.tar.gz` سرور با hash یکسان محفوظ‌اند، پس بازسازی ممکن است. فقط نسخه‌های موقت قابل‌بازتولید حذف شد؛ داده، `.private`، پشتیبان و تاریخچه دست‌نخورده‌اند. فضای آزاد پس از این کار 1,277,407,232 بایت بود؛ محدودیت ظرفیت همچنان مهم است.
- چک‌لیست مالک: `docs/operations/owner-integrated-test.md`. آزمون کامل تغییرات با دادهٔ مصنوعی فقط روی localhost؛ روی دامنه ورود/نمایش/مسیر خواندنی بررسی می‌شود و داده/تأیید آزمایشی به حساب مالک اضافه نمی‌شود. نتایج قطعی آزمون و Deploy در رسید ثبت می‌شود؛ پذیرش مالک باز است.
- شواهد مرورگر محلی: شاهد → کنترل ریسک → تأیید اقدام → ساخت متن → ویرایش ذخیره‌نشده (تأیید/خروجی بسته) → refresh با حفظ ویرایش → ذخیره → تأیید → دانلود → دریافت مجدد → یادگیری → reload و دانلود مجدد انجام شد. فایل‌های واقعی `pr-linkedin-draft-v4*.txt` در Downloads ساخته شدند؛ hash یکسان `84B540EB55619AC5FF77E28D99BF8F6C42F4102D579A440267E7678A620F1D83` و جملهٔ ویرایش‌شده در فایل احراز شد. رخداد download ابزار timeout داد، اما فایل واقعی و یکسانی محتوا مستقلاً بررسی شد؛ timeout ابزار به‌عنوان شکست دانلود محصول گزارش نمی‌شود. این داده‌ها و تأییدها صرفاً مصنوعی/محلی بودند. `pnpm check` کامل موفق شد؛ ۱۳ تست سرویس و ۷ تست رابط اختصاصی موفق‌اند؛ تست PostgreSQL نهایی در CI احراز می‌شود.
- Provider خاموش، بدون مهاجرت جدید یا خرید؛ وابستگی‌های بودجه/رضایت/اعتبارنامه، محدودیت تک‌مالک و HTTP اولیه و پشتیبان خارج‌سرور غیرزمان‌بندی‌شده همچنان برقرارند.

---

## انتشار بهبود تجربهٔ کاربری و راهنمای شروع — ۱۶ سپتامبر ۲۰۲۶

**مرجع نهایی نسخه/CI/Deploy/مرورگر:** `C:\Users\pc\.ssh\pr-preview\latest-release.json` و کپی همان رسید روی سرور `.private/latest-release.json`. این سند همراه کد commit می‌شود؛ رسید پس از انتشار به‌روزرسانی می‌شود تا SHA خودارجاعی نشود. تا تغییر رسید از `1088980`، تغییرات زیر را منتشرشده فرض نکنید.

### خروجی این مرحله

- رابط فارسی راست‌چین با منوی گروه‌بندی‌شده، منوی موبایل با عنوان، اندازهٔ متن خواناتر، کارت‌ها و دکمهٔ قدم بعد، focus صفحه‌کلید/پیوند عبور به محتوا و رعایت reduced-motion. هر ۱۷ بخش واقعی حفظ شده؛ کنترل‌های بی‌عمل تأییدها/پروفایل حذف شده‌اند.
- مرحلهٔ بعدِ مستقل از Provider تکمیل شد: راهنمای «تجربهٔ واقعی → هدف شخصی → انتخاب آگاهانه → متن مستند → تأیید و خروجی → بازخورد». وضعیت فقط از snapshotهای واقعی گرفته می‌شود؛ بازکردن صفحه، هدف پیش‌فرض، دادهٔ نخوانده، خروجی قدیمی یا لغو مجوز به معنی تکمیل نیست. انتخاب عدم اقدام به تولید محتوای اجباری تبدیل نمی‌شود. این راهنما جای پذیرش مالک یا تکمیل همهٔ Master Context را نمی‌گیرد.
- بازبینی امنیتی: رفع race تعویض verifier حین بررسی رمز و شمارش تلاش‌های موازی ورود؛ خواندن موفق و نگهداری read-only با بودجهٔ شکست قفل نمی‌شوند. HSTS سمت برنامه افزوده شد؛ هیچ اعتماد تازه‌ای به هدرهای proxy اضافه نشد.

### ایجنت‌ها و بازبینی واقعی

در شروع فقط `/root` فعال بود؛ نقش‌های زیر واقعاً در همین نوبت اجرا شدند، نه سرویس پس‌زمینهٔ دائمی:

| مسئول | محدودهٔ مستقل و نتیجه |
|---|---|
| `ux_implementation` | App/styles، منو/موبایل/خوانایی و اتصال راهنما؛ build موفق؛ اصلاح یافته‌های QA |
| `product_next_step` | مدل/کامپوننت راهنمای شروع و ۱۶ آزمون؛ دادهٔ کهنه/نامعلوم و عدم اقدام پوشش داده شد |
| `security_review` | چهار فایل ورود/آزمون، ۱۳ آزمون اختصاصی موفق؛ سپس بازبینی مستقل UI/مدل، بدون تغییر فایل‌های دیگران |
| `/root` | پشتیبان، هماهنگی مالکیت فایل، مرور تغییرات، آزمون مرورگر و کنترل نهایی CI/استقرار |

بازبین دو اشکال واقعی یافت: پیشرفت کهنه پس از لغو مجوز منبع و دکمهٔ ادامه روی همان صفحهٔ امروز. وضعیت خواندن وابسته‌ها اکنون با تغییرات باطل می‌شود و دکمه به بخش تصمیم‌ها focus/scroll می‌کند. نتیجهٔ کامل آزمون‌های نهایی در رسید ثبت می‌شود؛ گزارش یک ایجنت به‌تنهایی تأیید انتشار نیست.

### شواهد و مرز آزمون

- baseline قبل از کار: local/GitHub/server روی `108898028b6128ebcbe9f3aa815fead01f0f829b`؛ نه rollback و نه نسخهٔ دیگری مبنا قرار گرفت.
- پشتیبان محلی: `C:\Users\pc\Desktop\pr-audit-backups\pr-before-ux-team-20260916.tar.gz`؛ SHA256 `C9C25A588700C4BFA6880F71CAA113F7F0CCC1BDC76B213C1D29FFC61415DB71`؛ فایل‌های نامرتبط Tailscale و `%SystemDrive%` محفوظ‌اند.
- مرورگر محلی: اندازهٔ موبایل ۳۹۰×۸۴۴، عرض محتوا و viewport هر دو ۳۷۵ (بدون overflow افقی)؛ ثبت منبع ساختگی و هدف ساختگی، راهنما را واقعاً از منبع به هدف و سپس انتخاب اقدام برد. منوی موبایل پس از انتخاب بسته و focus به محتوا منتقل شد؛ ادامهٔ اقدام به `today-decisions` رفت. داده‌های این آزمون فقط در حافظهٔ API محلی‌اند، نه حساب Production مالک.
- ورود جدید فارسی قبلاً روی دامنه آزموده شده؛ پس از این انتشار، سلامت/ورود/نمایش و مسیر خواندنی دوباره مطابق رسید کنترل می‌شود. تأیید/دادهٔ آزمایشی روی Production ایجاد نمی‌شود و پذیرش واقعی مالک همچنان باز است.

### موانع و قدم بعد

- فضای اشتراکی سرور در بررسی زنده حدود ۱٫۴GB بود (df گرد‌شده ۱۰۰٪). زیر ۱GiB، پشتیبان برای حفاظت fail می‌شود. قبل از هر restart پشتیبان و restore ایزوله لازم است. هیچ داده، backup یا فایل پروژهٔ دیگری حذف نشد؛ برای دادهٔ حیاتی/حجیم به ظرفیت و نسخهٔ خارج سرور خودکار نیاز داریم.
- Provider، انتشار بیرونی و چندکاربری باز نشده‌اند؛ بودجه/اعتبارنامه/رضایت و ارزیابی route همچنان وابستگی واقعی‌اند. آزمون کامل محصول و baseline کیفیت با تجربهٔ واقعی مالک مرحلهٔ بعد پذیرش است.
- فقط URL امن HTTPS تحویل داده می‌شود. redirect اولیهٔ HTTP در لایهٔ میزبانی هنوز احراز نشده؛ formها HTTPS مطلق و cookieها Secure هستند. Chrome/Edge را بدون شاهد مرورگر واقعی آزموده اعلام نکنید.
- اسناد پایین این خط **سوابق تاریخی** هستند؛ برای روش ورود، نسخه، ظرفیت و نتیجهٔ فعلی، این بخش و رسید نهایی مقدم‌اند. روش فعلی ورود، فرم فارسی سایت است و اجرای فایل جدا لازم نیست.

---

## جایگزین ورود فارسی — ۱۶ سپتامبر ۲۰۲۶، آماده‌سازی

درخواست مالک: صفحهٔ ورود عادی فارسی به جای پنجرهٔ Basic Auth، بدون حذف حفاظت. کد شامل gate نشست تک‌مالک، cookie تصادفی HttpOnly/Secure/SameSite=Strict با پیشوند __Host، انقضای ۳۰ دقیقه عدم فعالیت/۸ ساعت مطلق، ابطال پس از خروج/restart/تعویض verifier، سقف تلاش و کنترل Origin برای ورود/خروج است. رمز فعلی حفظ می‌شود؛ هیچ ثبت‌نام عمومی یا Provider فعال نمی‌شود. مرورگر با Basic cache اجازهٔ دورزدن خروج ندارد؛ کلاینت صریح نگهداری همچنان سازگار است.

انتشار فقط پس از check/CI همان SHA، backup/restore و smoke نشست واقعی پشت محافظ قبلی انجام می‌شود؛ سپس `activate-session-login.sh` با پشتیبان مستقل proxy و بازگشت خودکار در شکست، صفحهٔ ورود را قابل‌دسترسی می‌کند. داده و API همچنان محافظت می‌شوند. نشست‌ها عمداً حافظه‌ای‌اند؛ restart ورود مجدد می‌خواهد و دادهٔ محصول PostgreSQL می‌ماند.

پشتیبان محلی قبل از تغییر: `C:\Users\pc\Desktop\pr-audit-backups\pr-before-form-login-20260916.tar.gz` شامل تاریخچه/تغییرات موجود، بدون node_modules/dist. HEAD/GitHub/server پیش از کار `87d5e6d3d0af3592c000823b732808365eaddf71` احراز شد. فضای آزاد سرور امروز حدود ۱٫۸GB است؛ حذف سایر داده‌ها مجاز نیست. نتیجهٔ نهایی آزمون/نسخه/مرورگر در رسید پس از انتشار ثبت خواهد شد؛ متن آماده‌سازی اثبات Deploy یا پذیرش مالک نیست.

## پیگیری بازنشدن مرورگر — ۱۵ سپتامبر ۲۰۲۶، ۲۳:۴۳ تهران

- خطای واقعی تب مالک در مرورگر داخلی مشاهده شد: `ERR_INVALID_AUTH_CREDENTIALS`، عنوان `This site can't be reached` برای دامنه PR. بنابراین تحویل مرورگری هنوز موفق نیست؛ پاسخ HTTP سالم به‌تنهایی اثبات نمایش/ورود/مسیر کاربر نیست.
- ابزار هنگام انتخاب همین تب، صفحهٔ خطای داخلی `data:` را با URL policy مسدود کرد؛ محدودیت دور زده نشد. API و قابلیت‌های اعلام‌شدهٔ مرورگر راهی برای ورود HTTP Basic یا پاک‌کردن credential ارائه نکردند. تلاش مجاز برای بازکردن دامنه در Chrome با `Browser is not available: chrome` خاتمه یافت؛ تنها مرورگر متصل IAB است، کنترل برنامهٔ بومی غیرفعال است.
- بررسی تازهٔ `verify-private-domain.ps1`: ناشناس 401؛ credential امن فعلی برای shell/asset و ۱۷ مسیر خواندنی 200؛ PostgreSQL persistent؛ probe بی‌اثر 403/415/404 موفق. رمز فعلی در HTTP پذیرفته می‌شود، ولی دلیل دقیق شکست چرخهٔ ورود IAB (عدم پشتیبانی prompt یا credential قدیمی) احراز نشده است.
- HEAD محلی `87d5e6d3d0af3592c000823b732808365eaddf71`؛ شناسهٔ استقرار همان رسید قبلی است و در این پیگیری مجدداً از سرور تطبیق داده نشد. هیچ تغییر محصول، ورود، سرور، Commit، Push یا Deploy انجام نشد؛ فقط این گزارش محلی تکمیل شد. Tailscale حفظ شد.
- مانع باز: ورود و آزمون مسیر اصلی در مرورگر واقعی انجام نشده؛ پذیرش مالک باز است. قدم بعد: اتصال یک مرورگر دارای ورود Basic به ابزار، یا ورود مالک در Chrome/Edge با credential امن موجود. تغییر مدل احراز هویت بدون ضرورت اثبات‌شده انجام نشد.
- پشتیبان سند پیش از ثبت: `C:\Users\pc\Desktop\pr-audit-backups\project-status-before-browser-diagnosis-20260915-2343.md`.

---

آخرین بررسی: ۲۰۲۶-۰۹-۱۵؛ پس از افزودن ورود داخلی و آزمون canary. زمان/شناسهٔ انتشار بعد از ثبت سند در رسید ماشین درج می‌شود. این گزارش، وضعیت تاریخی چت را مبنا فرض نمی‌کند.
**تحویل فنی: پایلوت تک‌مالک قابل‌آزمایش. پذیرش واقعی مالک: باز.**

## نسخه و شواهد جاری

| محور | نتیجهٔ احرازشده |
|---|---|
| checkout | `C:\Users\pc\Desktop\project\pr`، main، یک worktree؛ rollback یا نسخهٔ گمشده مشاهده نشد؛ فایل‌های نامرتبط Tailscale دست‌نخورده |
| انتشار قبل از محافظ ورود داخلی | `4be186eafbba52162a891b299002833b6ee5e9dd`؛ PostgreSQL، CSRF، ignore و سخت‌سازی حساب آزمون؛ روی دامنه احراز شد |
| CI انتشار قبلی | run `34961741865`، completed/success؛ verify و PostgreSQL integration/restore موفق |
| نامزد ورود داخلی آزموده‌شده | runtime/main.cjs SHA256 `1BE59A9DF5CA96200FB53D409E4EC849A6FC908CBBCFCFC63D53818583E60944`؛ canary: API بدون credential برابر 401، خواندن با token خصوصی برابر 200 و mutation با همان token برابر 401؛ Basic Auth واقعی مالک نیز در canary با verifier بومی آزموده شد |
| شناسهٔ نهایی کد/بسته/CI | از `SOURCE_COMMIT` سرور و `C:\Users\pc\.ssh\pr-preview\latest-release.json` بخوانید؛ نسخهٔ همان رسید روی سرور `.private/latest-release.json` است. این رسید بعد از انتشار با SHA بسته، CI همان SHA، hash باینری، smoke و backup ثبت می‌شود تا خودارجاعی SHA در commit سند ایجاد نشود |
| دامنه | https://pr.wealthos.ir؛ ورود ناشناس 401؛ ورود جدید، shell/asset و ۱۷ مسیر اصلی خواندنی 200؛ `/ready` برابر ready/postgres/persistent |
| امنیت مرورگر | probe بی‌اثر روی دامنه: Origin خارجی 403، text/plain برای mutation برابر 415، JSON از Origin مجاز به route ناموجود 404؛ CORS باز نشده |
| Basic Auth | تعویض bcrypt انجام و ورود جدید تأیید شد؛ verifier قبلی جایگزین شد، رمز قبلی از تاریخچه استخراج/آزموده نشد؛ بررسی forensic کامل دسترسی قبلی ادعا نمی‌شود |
| ورود داخلی برنامه | اعتماد صرف به loopback حذف شد: تمام APIهای Production هم credential مالک را با همان verifier Apache کنترل می‌کنند؛ رمز تنها از stdin به htpasswd می‌رود، نه argv/env/log. cache با تغییر verifier بی‌اعتبار می‌شود؛ اعتبارسنجی‌های هم‌زمان محدودند. token مستقل نگهداری در فایل 0600 زیر پوشهٔ 0700 فقط خواندن را مجاز می‌کند، نه mutation/تأیید انسانی؛ مقدار آن جایی گزارش نمی‌شود |
| محل ورود امن | `C:\Users\pc\.ssh\pr-preview\Open-PR-Login.cmd`؛ login با DPAPI، ACL فقط حساب مالک، خارج Git/چت |
| PostgreSQL | 16.15 اختصاصی PR، ۳۰ migration، pr_migrate/pr_runtime جدا؛ runtime بدون superuser/BYPASSRLS/CREATE؛ migration credential در PM2 وب نیست؛ Journal پایدار |
| مرز دسترسی | listenerهای API/PG فقط 127.0.0.1:31056/31556؛ `.private` اکنون 0700 بدون ACL ارثی، secrets 0600. ACL پوشهٔ ازقبل‌موجود قبل از اصلاح حفظ شد؛ secretها پیش از اصلاح نیز 0600 بودند |
| Provider | providerConfigured=false، executionEnabled=false؛ شبکهٔ اتصال‌ها و انتشار بیرونی خاموش؛ بودجه/رضایت بیرونی از پیش‌فرض‌ها استنتاج نشده |

CI انتشار قبلی: https://github.com/tparkhondeh/pr/actions/runs/34961741865؛ CI و نسخهٔ نهایی در رسید ماشین پیوند دارند.

## آزمون و داده

- آخرین `pnpm check`: ۳۶۸ تست در ۶۳ فایل و ۱۲۲ case ارزیابی موفق، صفر شکست؛ شامل ۱۴ تست محافظ مرورگر، ۵ تست ورود داخلی و ۳۲ تست HTTP. کانری مستقل روی PostgreSQL واقعی با credential فعلی مالک و ابزار نگهداری نیز موفق است. CI کل suite، build و security scan/audit برای SHA نهایی باید مطابق رسید success باشد؛ آمار تست با پذیرش واقعی یکی نیست.
- integration واقعی PostgreSQL روی DB جدا `pr_acceptance_1789467922576`: ۳۰ migration و RLS enforced. پس از اصلاح cleanup، دوباره روی `pr_acceptance_1789470497911` موفق و `pr_app_test` به NOLOGIN تبدیل شد؛ دادهٔ آزمون حفظ می‌شود ولی credential ثابت آزمون، login فعال باقی نمی‌گذارد. هیچ سناریوی ساختگی در حساب Production مالک ساخته نشد.
- UI محلی: رضایت → asset/evidence → سه پیشنهاد شامل عدم اقدام → بررسی ریسک → draft مستند → تأیید آزمایشی → export؛ ویرایش بعدی تأیید را باطل و سیگنال یادگیری ثبت کرد. بعد از محافظ مرورگر، ذخیرهٔ هدف از UI نیز موفق بود. این تأییدها پذیرش مالک نیستند.
- پیش از انتقال، export کامل memory خصوصی ذخیره شد: `pre-postgres-1789469106950.json` زیر `.private`؛ صفر memory/asset/evidence و فقط audit خروجی‌گیری عامل. دادهٔ واقعی حذف یا جایگزین نشد.
- backup قبل از کار محلی: `C:\Users\pc\Desktop\pr-audit-backups\pr-before-mvp-20260915-134103.tar.gz`؛ SHA256 `CFB415A7A840B2439FB81A0633A4D968AD7C78D22D88D9A99F2BF27B84847BE5`؛ شامل `.git`، بدون node_modules قابل نصب مجدد.
- backup پیش از هر انتشار در `.deploy-backups/release-before-<SHA>/previous-release.tar.gz`. rollback کد، PostgreSQL و دادهٔ جدید را نگه می‌دارد؛ بازگشت به memory مجاز نیست.
- backup پایدار تأییدشده: `.private/backups/pr-20260915110153235.dump.enc`؛ AES-256-GCM، SHA256 `2d76893f7490ee1e1f8a73e4bfc9a6800c6894d08f685545a3741e21700f7a35`؛ restore ایزوله و fingerprint برابر ۵۳ جدول/journal، ۰٫۹۷ ثانیه مرحلهٔ restore (نه RTO کامل سرویس).
- همان backup و manifest خارج سرور: `C:\Users\pc\.ssh\pr-preview\backups\20260915-143335`؛ checksum برابر؛ مواد بازیابی رمزگذاری‌شده در `recovery-20260915-143335.clixml` همان پوشهٔ امن اصلی، با آزمون DPAPI roundtrip.
- وب و PostgreSQL **فقط PR** جداگانه restart شدند؛ readiness بازگشت و fingerprint هر ۵۳ جدول/journal قبل/بعد restart PG برابر بود. کل میزبان reboot نشد.
- crontab قبلی backup و حفظ شد؛ backup رمزگذاری‌شده روزانه 02:17 تهران و recovery فقط PR در reboot ثبت شد. اجرای دستی recovery موفق؛ اولین اجرای زمان‌بندی‌شده هنوز شاهد مستقل ندارد.

## راهنمای کوتاه مالک

۱. `Open-PR-Login.cmd` را در ویندوز دوبار کلیک کنید؛ اطلاعات ورود فقط محلی نمایش داده می‌شود. دامنه را ترجیحاً در پنجرهٔ خصوصی مرورگر باز کنید و رمز جدید را جایگزین رمز قبلی کنید.

۲. «شروع و منابع»: یک تجربهٔ کوتاه و غیرحساس با رضایت استفاده ثبت کنید. «حافظه من» و «استراتژی»: مدل/هدف را ببینید و اصلاح کنید.

۳. «امروز»: سه گزینه را مقایسه کنید؛ مسیر محتوا و ریسک‌هایش را تأیید کنید. «پیش‌نویس»: شاهد → ویرایش → تأیید → خروجی. سپس «یادگیری» و «داده و شفافیت» را امتحان کنید.

## محدودیت‌های مهم و مسیر بعد

| مرحله | خروجی و معیار پایان | وابستگی / اقدام مالک |
|---|---|---|
| آزمون واقعی پایلوت | مالک مسیر بالا را با تجربهٔ واقعی طی کند؛ صحت/فایده/اصالت و حقوق داده را بررسی کند | اولین قدم فعلی؛ نتیجه/اشکال را بگوید. هیچ پذیرش واقعی ثبت نشده |
| نگهداری قابل‌اتکا برای دادهٔ حیاتی | ظرفیت رشد، retention، نسخهٔ خارج سرور خودکار و بازیابی کامل سرویس احراز شود | اکنون فقط حدود ۶٫۷GB آزاد/۹۷٪ مصرف. هیچ فایل سایر پروژه‌ها حذف نشد؛ برای شروع متن کوچک، نه دادهٔ حیاتی یا حجیم |
| Provider کنترل‌شده | account/secret امن + رضایت پردازش + سقف هزینهٔ مشخص + eval هر route + metering/reconciliation و توقف | اطلاعات و انتخاب مالک هنوز موجود نیست؛ بدون آنها فعال نمی‌شود. draft فعلی قطعی/شاهد‌محور است؛ عدد سقف پیش‌فرض بودجهٔ تأییدشده نیست |
| کاربران دیگر | ورود/session مستقل و نگاشت هویت معتبر، سپس آزمون جداسازی واقعی | پایلوت فعلی فقط یک مالک با Basic Auth است؛ RLS آزموده شده اما حساب مشترک مجوز multi-user نیست |
| توسعهٔ بلندمدت | research/radar واقعی، چندرسانه‌ای/برداری، روابط و ادراک چندمنبعی، اتصال پلتفرم‌ها، crisis و proactive | scope/رضایت/هزینه و پذیرش جدا؛ حذف نشده‌اند ولی مانع تحویل پایلوت نیستند |

backup روزانه روی همین سرور است؛ نسخهٔ خارج سرور فعلاً snapshot دستی احرازشده، نه انتقال روزانه.
RPO خرابی کامل سرور از آخرین انتقال خارج سرور محاسبه می‌شود. DPAPI به همین حساب ویندوز و کلیدهای آن وابسته است.
PITR، HA، حذف خودکار retention، RTO بازیابی کامل و reboot واقعی میزبان احراز نشده‌اند؛ بازیابی snapshot تنها برابری داده/ساختار را اثبات می‌کند.
زیر ۱GiB فضای آزاد، backup برای حفاظت دیسک fail و خطا ثبت می‌کند. حذف/retention خودکار دادهٔ واقعی بدون تصمیم تازه اجرا نمی‌شود.
TCP probe از این شبکه حتی برای پورت استفاده‌نشده handshake نشان داد، ولی HTTP مستقیم صفر بایت/timeout بود؛ اثبات firewall عمومی از آن استنتاج نشده است. شاهد binding محلی مستقل تأیید شده است.
محافظ داخلی جایگزین جداسازی حساب سیستم‌عامل نیست: پردازش‌های دارای همان حساب `wealthos_dev` یا root همچنان به فایل‌های خصوصی آن حساب دسترسی دارند. ایزوله‌سازی سیستم‌عاملی کامل PR از سایر برنامه‌ها به حساب/اختیار مدیریتی جدا نیاز دارد؛ چنین اختیاری در این نوبت موجود نبود و سرویس‌های دیگر دستکاری نشدند.

**تعریف پایان:** تحویل فنی این پایلوت با پذیرش واقعی مالک و تکمیل کل Master Context یکی نیست. کار مستقلِ ضروری این تحویل انجام شده؛ اولین اقدام مفید بعد، آزمون واقعی مالک و سپس رفع یافته‌هاست. مجوز کلی ادامه دوباره درخواست نمی‌شود.

جزئیات تاریخی فقط در صورت نیاز: `project-status-history-2026-09-15.md`. راهنمای ریشه و ترجیح گزارش کوتاه در AGENTS.md ثبت و به Git/سرور منتقل شده؛ بارگذاری خودکار چت بدون دسترسی به این checkout تضمین نمی‌شود.
