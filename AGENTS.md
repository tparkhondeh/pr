# PR — راهنمای پایدار همکاری

## قاعدهٔ مصرف توکن تا پایان پروژه

تا پایان این پروژه، در تمام نوبت‌ها و چت‌های مرتبط، توکن را بدون افت کیفیت، دقت، پوشش نیازها یا راندمان بهینه مصرف کن. ابتدا آخرین وضعیت و تغییرات پس از آن را بخوان؛ فقط برای رفع ابهام به گذشته برگرد. خواندن‌ها و جستجوهای مستقل را دسته‌بندی و خروجی‌ها را کوتاه و مرتبط کن. فایل‌های بدون تغییر، لاگ کامل، برنامه‌های تکراری و وضعیت‌های بدون تغییر را دوباره مصرف نکن. تست‌های متناسب و تمام کنترل‌های ضروری امنیت، حریم خصوصی، داده و انتشار را کامل انجام بده؛ تکرار فقط با تغییر تازه، شکست یا ابهام موجه باشد. برای صرفه‌جویی، کنترل ضروری را حذف نکن و کار را ناقص تحویل نده. در نقاط عطف و پیش از انتقال به چت بعدی، نسخهٔ کد و استقرار، شواهد، موانع و قدم بعدی را کوتاه ثبت کن. از کاربر نخواه این قاعده را دوباره تکرار کند.

## نقطهٔ شروع و ثبت شواهد

- گزارش‌های مالک را فارسی، بسیار کوتاه و غیرتخصصی بنویس: چه چیزی آماده شد، کجا و چگونه امتحان شود و آیا اقدام ضروری لازم است. جزئیات فنی و شواهد را در گزارش پایدار نگه دار؛ مشکل مهم، هزینه یا خطر داده را پنهان نکن. این ترجیح را بدون درخواست تکرار از مالک رعایت کن.

- ابتدا `docs/operations/project-status-and-roadmap.md` و سپس تغییرات پس از زمان آن را بررسی کن؛ گزارش تاریخی، وضعیت زنده نیست.
- موفقیت تست، شناسهٔ استقرار، آزمون دامنه و پذیرش صریح مالک را جدا ثبت کن. هیچ رمز، توکن یا محتوای فایل رمز را وارد گزارش نکن.
- در هر نقطهٔ عطف، گزارش وضعیت را با زمان، SHA کد/استقرار، شواهد، موانع، تصمیم‌های باز و اولین قدم بعدی به‌روزرسانی کن؛ برای رفع اختلاف فقط منابع لازم را بخوان.
- این راهنما مجوز توسعه، Commit، Push، Deploy، تغییر اطلاعات ورود یا فعال‌سازی Provider نیست؛ حدود آخرین درخواست کاربر ملاک است.
- فایل‌های نامرتبط Tailscale و تغییرات کاربر را حفظ کن. برای اصلاح یا بازیابی، نخست وضعیت را پشتیبان بگیر؛ نسخهٔ گمشده را ابتدا جدا بررسی کن و تاریخچه را بازنویسی نکن.

## محدودهٔ بارگذاری

این فایل راهنمای ریشهٔ همین checkout است و از انتشار ۱۵ سپتامبر ۲۰۲۶ در Git و بستهٔ سرور نیز قرار دارد. در نوبت اجرایی مستقیماً خوانده و اعمال شده است. بارگذاری خودکار در جلسه‌های بعد به کشف راهنمای پروژه توسط محیط بستگی دارد؛ چت بدون دسترسی به این مسیر، checkout قدیمی یا میزبان دیگر الزاماً آن را دریافت نمی‌کند. راهنمای ریشه را در نسخه‌های کاری آینده نیز منتقل و بارگذاری آن را بررسی کن؛ از مالک نخواه قاعده را دوباره بنویسد.
